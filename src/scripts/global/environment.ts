import { DEFAULT_GLOBAL_MEMORY } from '../lib/memory/memory.constants';
import { GlobalMemory } from '../types';

export const DEBUG_ENABLED = false;
export const MEMORY_SEED: GlobalMemory = DEFAULT_GLOBAL_MEMORY;
